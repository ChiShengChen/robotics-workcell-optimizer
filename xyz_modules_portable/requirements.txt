# Core (always required)
pydantic>=2.5
numpy>=1.26

# DXF read/write (cad_import, export_cad)
ezdxf>=1.3

# Image floor-plan detection (image_floor_plan)
opencv-python-headless>=4.10

# 3D mesh output (export_cad write_stl)
trimesh>=4.0

# OPTIONAL — 3D BREP output (export_cad write_step). Heavy (~200 MB wheel).
# Drop the line if you don't need STEP files.
cadquery>=2.7

# OPTIONAL — hybrid image floor-plan mode (CV + Gemini Vision judgement).
# Drop if you only use 'cv' / 'auto' / 'hough' modes.
google-genai>=0.3

# NOTE — DWG output (export_cad write_dwg) calls an external binary
# (LibreDWG's `dxf2dwg` or ODA File Converter). It is NOT a pip dep.
# Install separately; see "DWG install" in README_HANDOFF.md.
